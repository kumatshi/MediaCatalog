<Window x:Class="MediaCatalog.Views.AddMediaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Добавить медиа"
        Height="800"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F0F12"
        Name="AddMediaDialogWindow">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
                <materialDesign:BundledTheme BaseTheme="Dark"
                                             PrimaryColor="DeepPurple"
                                             SecondaryColor="Amber"/>
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="CardBackground" Color="#1B1B22"/>
            <SolidColorBrush x:Key="MutedText" Color="#A6A6B3"/>           <SolidColorBrush x:Key="ErrorBrush" Color="#E57373"/>
            <SolidColorBrush x:Key="InputBackground" Color="#2A2A36"/>
            <SolidColorBrush x:Key="InputBorder" Color="#3A3A4A"/>
            <SolidColorBrush x:Key="InputFocusedBorder" Color="#FFC107"/>
            <Style TargetType="TextBox">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource InputBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Padding" Value="6"/>
                <Setter Property="Margin" Value="0 0 0 10"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="CaretBrush" Value="White"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource InputFocusedBorder}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>


            <Style TargetType="ComboBox">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource InputBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Padding" Value="6"/>
                <Setter Property="Margin" Value="0 0 0 10"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource InputFocusedBorder}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource MutedText}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="0 0 0 6"/>
            </Style>


            <Style TargetType="Button" x:Key="DialogButtonStyle">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Width" Value="100"/>
                <Setter Property="Margin" Value="5,0,0,0"/>
                <Setter Property="Background" Value="#2A2A36"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3A3A4A"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#5A5A6A"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="#1B1B22"/>
                        <Setter Property="Foreground" Value="#555"/>
                        <Setter Property="Cursor" Value="Arrow"/>
                    </Trigger>
                </Style.Triggers>
            </Style>


            <Style x:Key="OmdbButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
                <Setter Property="Background" Value="#673AB7"/>

                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Width" Value="120"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#9575CD"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#4527A0"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

  
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Тип:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1"
                  Name="MediaTypeComboBox"
                  SelectionChanged="MediaTypeComboBox_SelectionChanged"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Название:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Name="TitleTextBox"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Год:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Name="YearTextBox"
                 Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Жанр:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Name="GenreTextBox"
                 Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Рейтинг:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Name="RatingTextBox"
                 Text="{Binding Rating, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Name="SpecificFieldsPanel"
                    Margin="0 10"/>


        <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                   Name="ValidationMessageText"
                   Text="{Binding ValidationMessage}"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap"
                   Margin="0 10"/>


        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Найти на OMDb"
                    Name="FindOmdbButton"
                    Click="FindOmdbButton_Click"
                    Margin="0 0 10 0"
                    Style="{StaticResource OmdbButtonStyle}"/>


            <Button Content="Сохранить"
                    Name="SaveButton"
                    Click="SaveButton_Click"
                    IsDefault="True"
                    IsEnabled="{Binding IsValid}"
                    Style="{StaticResource DialogButtonStyle}"/>

            <Button Content="Отмена"
                    Name="CancelButton"
                    Click="CancelButton_Click"
                    IsCancel="True"
                    Margin="5,0,0,0"
                    Style="{StaticResource DialogButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
